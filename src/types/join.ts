export interface InvalidType {
  input: boolean;
  confirm: boolean;
}

export interface PostingUserInfo {
  email: string;
  password: string;
  nickname: string;
  birthday: string;
  gender: string;
  imgFile: File;
}

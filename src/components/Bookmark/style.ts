import styled from "styled-components";

export const St = {
  List: styled.section`
    display: flex;
    flex-direction: column;
    gap: 0.8rem;

    padding: 0 1.6rem;
    margin-bottom: 3.6rem;
  `,
};
